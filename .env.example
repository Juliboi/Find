# Public Supabase config (safe to ship in mobile app)
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Server-only secret used by the schedule-day Supabase Edge Function.
# DO NOT prefix with EXPO_PUBLIC_. Set it in `supabase secrets set OPENAI_API_KEY=...`.
OPENAI_API_KEY=
